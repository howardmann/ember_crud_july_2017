
import { datetime } from 'ember-book/helpers/datetime';
import { module, test } from 'qunit';

module('Unit | Helper | datetime');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = datetime([42]);
  assert.ok(result);
});

